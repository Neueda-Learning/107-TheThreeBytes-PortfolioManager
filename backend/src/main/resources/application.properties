spring.application.name=hsbcproject

# --- MySQL (matches schema.sql database: portfolio_db) ---
spring.datasource.url=jdbc:mysql://localhost:3306/portfolio_db
spring.datasource.username=root
spring.datasource.password=n3u3da!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
server.error.include-message=always

# --- Finnhub API (used for STOCK tickers only) ---
finnhub.api.key=d9pmunpr01qi2fkvd5p0d9pmunpr01qi2fkvd5pg
finnhub.cache.quote-ttl-seconds=60
finnhub.cache.candle-ttl-hours=24

# --- CORS (frontend dev server) ---
app.cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173